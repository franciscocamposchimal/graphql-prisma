const User = {
    posts(parent, args, ctx, info){
        //parent.id
        return [];
    },
    comments(parent, args, ctx, info){
        //parent.id
        return [];
    }
};

export { User as default };